type Task @model @key (name:"task",fields:["teamID","title"]){
  id: ID!
  teamID:ID!
  title: String!
  body: String
  state: String
  teams:Team! @connection( fields :["teamID"])
}
type Team @model {
  id :ID!
  name : String!
  tasks:[Task] @connection (keyName: "task",fields:["id"])
}
